# They Are Here Pipeline Dependencies
# ---------------------------------------------------------
# Pinning strategy: ~=X.Y.0 allows patch updates (X.Y.*)
# but blocks minor/major bumps that could break the pipeline.
# To upgrade, bump the version floor and test locally first.
# ---------------------------------------------------------
# For optional integrations (AWS, Prefect, Reddit, social):
#   pip install -r requirements-optional.txt
# ---------------------------------------------------------

# Core Data Processing
pandas~=3.0.0
numpy~=2.4.0
requests~=2.32.0
sentence-transformers~=5.2.0
hdbscan~=0.8.33
scikit-learn~=1.8.0
torch~=2.10.0

# Web Scraping
beautifulsoup4~=4.12.0
feedparser~=6.0.0
pdfplumber~=0.11.0

# Automation
apscheduler~=3.10.0

# Topic Modeling & NLP
bertopic~=0.16.0
umap-learn~=0.5.0
spacy~=3.7.0
thinc>=8.2.2,<8.3.0        # must build against numpy 2.x; floor ensures compat

# PII Protection
presidio-analyzer~=2.2.0
presidio-anonymizer~=2.2.0

# Real-time Communication
websockets~=16.0.0

# Geo-Intelligence
shapely~=2.0.0; python_version >= "3.9"
geopandas~=1.0.0; python_version >= "3.9"

# Date utilities
python-dateutil~=2.9.0

# Analytical Substrate
duckdb~=1.4.0
